import { Routes } from '@angular/router';
import { PomodoroContainer } from './pomodoro-container/pomodoro-container';

export const POMODORO_ROUTES: Routes = [
  { path: '', component: PomodoroContainer }
];
